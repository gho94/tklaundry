# 수동 테스트 체크리스트

각 항목 결과: **통과 / 실패 / 미구현**

---

## 준비

| 항목 | 내용 |
|------|------|
| DB | `TKLaundry` (레거시와 동일) |
| 레거시 | `TKLaundry.exe` + 소스 폴더 |
| 차세대 | API + Flutter (`localhost:8080`) |
| 계정 | `ComMember` `UseYn='Y'` 1개 |
| 로그 | `backend/logs/tklaundry-api.log` |

---

## 0단계

| # | 확인 | 기대 |
|---|------|------|
| 0-1 | 공통 위젯 | 버튼, 입력, 콤보, 그리드, 사이드바 등 (업무 화면에서 사용) |
| 0-2 | API 기동 | `application.yml`로 DB 연결 (`localhost`) |
| 0-3 | 로그 | traceId 포함 (오류 JSON·로그 파일 일치) |

---

## 1-0. 셸 (메뉴·레이아웃)

| # | 확인 | 기대 |
|---|------|------|
| S-1 | 메뉴 전환 | 코드·사용자·고객·제품 화면 표시 |
| S-2 | 미구현 메뉴 | 접수·출고 등 → placeholder |
| S-3 | 로그아웃 | 로그인 화면 · `codeProvider` 초기화 |

> 1단계: 메뉴 전환 시 화면 dispose (상태 유지·탭 **없음**). 2단계부터 비즈니스만 캐시·탭 검토 — [02_아키텍처.md](02_아키텍처.md)

---

## 1-1. 회원

| # | 확인 | 기대 |
|---|------|------|
| 1-1 | 로그인 | 빈 값·틀린 비번 차단 |
| 1-2 | 로그인 성공 | 메인 이동, 상단 사용자명 표시 |
| 1-3 | authProvider | `userId`·`userName` 저장 |
| 1-4 | 자동 로그인 | 체크 후 로그인 → 앱 재실행 시 메인 진입 |
| 1-5 | 신규 가입 | 로그인 화면 가입 → 별도 로그인 없이 메인 진입 |
| 1-6 | 회원 CRUD | 목록·등록·수정·삭제·`UseYn` 레거시와 DB 동일 |
| 1-7 | 본인 삭제 | `409 CONFLICT` |

**API 수동 확인**

```powershell
# 목록
curl http://localhost:8080/api/members

# 아이디 중복 확인 (true = 이미 존재)
curl "http://localhost:8080/api/members/exists?userId=test01"

# 회원가입 (201 + member body)
curl -X POST http://localhost:8080/api/auth/register -H "Content-Type: application/json" -d "{\"userId\":\"test01\",\"password\":\"pw\",\"userName\":\"테스트\",\"useYn\":\"Y\"}"

# 수정 (password 생략 시 기존 비밀번호 유지, 204)
curl -X PUT http://localhost:8080/api/members/test01 -H "Content-Type: application/json" -d "{\"userName\":\"테스트수정\",\"useYn\":\"Y\"}"

# 삭제 (204)
curl -X DELETE http://localhost:8080/api/members/test01
```

> 관리용 **단건 GET** (`GET /api/members/{userId}`) **없음** — 수정은 목록 행·앱 그리드 기준.

레거시 `FrmMember`와 DB `ComMember` 행이 동일한지 대조한다.

---

## 1-2. 공통코드

| # | 확인 | 기대 |
|---|------|------|
| 2-1 | login 캐시 | login 응답 `codes` · `codeProvider` 적재 · 로그아웃 초기화 |
| 2-2 | 목록 API | `GET /api/codes` flat JSON |
| 2-3 | 트리 UI | ROOT 하위 구조 일치 (설정→코드) |
| 2-4 | 선택 상세 | `codeProvider` flat — ID·코드명·상위 ID·등급 |
| 2-5 | 등록 | API·UI — 최상위·하위 채번 · 트리 갱신 |
| 2-6 | 수정·삭제 | API·UI — 하위 재귀 삭제 · CRUD 후 `codeProvider` 갱신 |
| 2-7 | 캐시 반영 | 코드 화면 진입 시 캐시 있으면 별도 목록 조회 없음 |

**API 수동 확인**

```powershell
# 로그인 (200, user + codes)
curl -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d "{\"userId\":\"admin\",\"password\":\"...\"}"

# 목록 (flat, 캐시 갱신용)
curl http://localhost:8080/api/codes

# 최상위 등록 (201)
curl -X POST http://localhost:8080/api/codes -H "Content-Type: application/json" -d "{\"pCodeId\":\"Root\",\"codeName\":\"테스트분류\"}"

# 하위 등록 (201)
curl -X POST http://localhost:8080/api/codes -H "Content-Type: application/json" -d "{\"pCodeId\":\"A00001\",\"codeName\":\"하위코드\"}"

# 수정 (204)
curl -X PUT http://localhost:8080/api/codes/A00001 -H "Content-Type: application/json" -d "{\"codeName\":\"변경된 코드명\"}"

# 삭제 (204, 하위 포함)
curl -X DELETE http://localhost:8080/api/codes/A00001

# 필수값 누락 (400 VALIDATION_ERROR)
curl -X POST http://localhost:8080/api/codes -H "Content-Type: application/json" -d "{\"pCodeId\":\"Root\"}"
```

응답 예 (login): `{ "user": { "userId": "admin", "userName": "관리자" }, "codes": [ ... ] }`

응답 예 (등록 201): `{ "codeId": "A10004", "pCodeId": "A00001", "codeName": "하위코드" }`

**앱 확인**

- [ ] **로그인** → 코드 화면 즉시 트리 표시 (login 캐시)
- [ ] **로그아웃** → `codeProvider` 초기화
- [ ] **최상위·하위 추가** → 트리 갱신
- [ ] **수정** → flat 캐시 기준 다이얼로그 · 저장 후 트리 반영
- [ ] **삭제** → 하위 포함 확인 · subtree 제거
- [ ] 실패 시 API 메시지·`traceId` 표시

레거시 코드관리(`FrmBaseData`)와 DB `ComBaseData` 행·트리 구조가 동일한지 대조한다.

---

## 1-3. 고객

| # | 확인 | 기대 |
|---|------|------|
| 3-1 | 목록·필터 | `aptCode` 생략=전체 · 특정 아파트 · `aptCode=` 기타 · 정렬 레거시와 동일 |
| 3-2 | 등록 | 채번·4코드·이름·전화 DB 일치 · 동일 위치 `409` |
| 3-3 | 수정 | 더블클릭 다이얼로그 · `PUT` 반영 · 중복 검사 없음 |
| 3-4 | 삭제 | 선택·확인 후 `DELETE` · 목록 갱신 |
| 3-5 | 코드 콤보 | 아파트(`A10001` 하위)·동(아파트 하위)·층(`A10002`)·호(`A10003`) |

**API 수동 확인**

```powershell
# 전체 목록
curl http://localhost:8080/api/customers

# 아파트 필터
curl "http://localhost:8080/api/customers?aptCode=A20001"

# 기타 (빈 AptCode)
curl "http://localhost:8080/api/customers?aptCode="

# 등록 (201)
curl -X POST http://localhost:8080/api/customers -H "Content-Type: application/json" -d "{\"custName\":\"테스트 1-101\",\"aptCode\":\"A20001\",\"buildingCode\":\"A30001\",\"floorCode\":\"A20001\",\"roomCode\":\"A20001\",\"custPhone\":\"010\"}"

# 수정 (204)
curl -X PUT http://localhost:8080/api/customers/C0001 -H "Content-Type: application/json" -d "{\"custName\":\"테스트 수정\",\"aptCode\":\"A20001\",\"buildingCode\":\"A30001\",\"floorCode\":\"A20001\",\"roomCode\":\"A20001\",\"custPhone\":\"\"}"

# 삭제 (204)
curl -X DELETE http://localhost:8080/api/customers/C0001
```

**앱 확인**

- [ ] 아파트 콤보(전체·기타) · 조회 · 그리드 스크롤
- [ ] **등록** — 연쇄 콤보·이름 자동 조합·저장 후 목록 반영
- [ ] **더블클릭** — 행 값으로 수정 다이얼로그 · 저장 후 반영
- [ ] **삭제** — 선택·확인·목록에서 제거
- [ ] 실패 시 API 메시지·`traceId` 표시

> **알려진 이슈**: 앱 최초 진입 시 `codeProvider` 비어 있으면 콤보 빈 상태 — 코드 화면 조회 후 정상화 (추후 수정 예정)

레거시 `FrmCustomer`·`FrmCustomerInsert`와 DB `ComCustomer` 행이 동일한지 대조한다.

---

## 1-4. 제품

| # | 확인 | 기대 |
|---|------|------|
| 4-1 | 목록·필터 | 공정·그룹 **둘 다 선택** 후 목록 · `ORDER BY ProductCode` |
| 4-2 | 등록 | 채번 `P`+4자리 · 공정·그룹=현재 필터 · DB 일치 |
| 4-3 | 수정 | 더블클릭 · 제품명·단가만 · `PUT` 204 |
| 4-4 | 삭제 | 선택·확인 · `DELETE` 204 · 목록 갱신 |
| 4-5 | 코드 콤보 | 공정(`B10001` 하위)·그룹(공정 하위) · `codeProvider` |

**API 수동 확인**

```powershell
# 목록 (processCode·groupCode 필수)
curl "http://localhost:8080/api/products?processCode=B20003&groupCode=B30001"

# 등록 (201)
curl -X POST http://localhost:8080/api/products -H "Content-Type: application/json" -d "{\"processCode\":\"B20003\",\"groupCode\":\"B30001\",\"productName\":\"와이셔츠\",\"price\":3000}"

# 수정 (204)
curl -X PUT http://localhost:8080/api/products/P0001 -H "Content-Type: application/json" -d "{\"productName\":\"와이셔츠(수정)\",\"price\":3500}"

# 삭제 (204)
curl -X DELETE http://localhost:8080/api/products/P0001
```

> 단건 **GET** 없음 — 수정은 목록 행·앱 그리드 기준.

**앱 확인**

- [ ] 공정→그룹 연쇄 콤보 · 조회 · 그리드
- [ ] **등록** · **더블클릭 수정** · **삭제**
- [ ] 실패 시 API 메시지·`traceId` 표시

레거시 `FrmProduct`와 DB `ComProduct` 행이 동일한지 대조한다.

> **1단계 통과 후** 2단계.

---

## 2단계. 접수 · 출고 · 매출

| # | 확인 | 기대 |
|---|------|------|
| 5-1 | 접수 목록 | **건수·총액 레거시 일치** |
| 5-2 | 접수 상세·상태변경 | DB 일치 |
| 6-1 | 출고 | 레거시 동일 |
| 7-1 | 매출 | 수납 규칙 동일 |
| 5-3 | 오류 시 | 화면 traceId = 로그 traceId |

---

## 롤백

| # | 확인 | 기대 |
|---|------|------|
| 8-1 | API·앱 중지 | 레거시만으로 업무 가능 |
| 8-2 | 재기동 | 정상 복구 |
