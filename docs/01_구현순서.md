# 범위 · 구현 순서

---

## 목표

- 레거시와 **같은 업무**를 차세대에서 처리
- **기초 마스터 4개**를 레거시와 맞춘 뒤 접수·출고·매출 진행
- 문제 생기면 차세대만 끄고 레거시로 계속 (롤백)

## 하지 않는 것

- 고객용 앱/웹, 클라우드, 멀티매장, 외부 API 공개

---

## 현재 진행 (2026-07)

| 단계 | 상태 | 비고 |
|------|------|------|
| 0단계 | **완료** | 뼈대·공통 위젯·로그 |
| ① 회원 | **완료** | API·Flutter·자동로그인·가입 후 즉시 로그인 |
| ② 공통코드 | **완료** | CRUD·login `codes` 캐시·전역 `codeProvider` |
| ③ 고객 | **완료** | API·Flutter 목록·등록·수정·삭제 |
| ④ 제품 | **완료** | API·Flutter 목록·등록·수정·삭제 |
| **1단계** | **완료** | 기초 마스터 4개 |
| 2단계 | **미착수** | 접수·출고·매출 |

---

## 0단계 — 뼈대 잡기

- [x] 저장소 구조 (`docs/`, `backend/`, `app/`)
- [x] Java: Spring Boot + MyBatis + `I*Service` / `*Service` 골격
- [x] **YAML 설정**: `application.yml` 하나 (profile 없음, `localhost`)
- [x] **로그**: 오류 JSON traceId · `logs/` 롤링 (요청 전구간 MDC·필터는 추후) — [02_아키텍처.md](02_아키텍처.md)
- [x] Flutter: Riverpod + go_router + Theme · 공통 위젯 (`Tk*`)

---

## 1단계 — 기초 마스터 (접수 전에 끝내기)

레거시 화면·DB 결과가 **같을 때까지**. 접수는 1단계 **이후**.

### ① 회원 (`ComMember`) — 완료

- [x] 로그인 (`user` + `codes` 응답 · `authProvider` + `codeProvider` 적재)
- [x] 자동 로그인 (`shared_preferences` · 앱 재실행 시 login API 재호출)
- [x] 회원가입 (`201` + member 반환 · 로그인 화면 가입 시 즉시 로그인)
- [x] 아이디 중복 확인 (`GET /api/members/exists`)
- [x] 회원 목록·수정·삭제·`UseYn` (상세 GET 없음 — 목록 행으로 수정 다이얼로그)
- [x] Flutter: 로그인 · 회원 목록(`MemberListPage`) · 등록/수정 다이얼로그

### ② 공통코드 (`ComBaseData`) — 완료

**목록·트리 UI**

- [x] `GET /api/codes` flat 목록
- [x] Flutter: `codeProvider` · 트리+상세 UI · 설정→코드 메뉴

**등록**

- [x] `POST /api/codes` (`pCodeId`·`codeName`만 · `codeId` 서버 채번 · `@Valid`)
- [x] Flutter: 최상위·하위 추가 다이얼로그

**수정·삭제**

- [x] `PUT /api/codes/{codeId}` · `DELETE /api/codes/{codeId}` (하위 재귀 삭제)
- [x] Flutter: 수정(flat 캐시) · 삭제 확인 · 상세 패널 버튼
- [x] 수정·삭제는 사전 `NOT_FOUND` 조회 없음 (화면 선택 코드만 처리)

**캐시**

- [x] login 응답 — `CommonInfo`에 `user` + `codes` flat (별도 DTO 없음)
- [x] Flutter: 로그인·자동로그인 → `codeProvider.setCodes()` · 로그아웃 → `clear()` (고객·제품 콤보 즉시 사용)
- [x] 코드 CRUD 후 `codeProvider.search()` 전역 갱신
- [x] 단건 `GET /api/codes/{codeId}` 제거 — 상세·수정은 flat 캐시

**Flutter 공통 UI (1단계 마스터 후 점진 정리)**

- [x] SnackBar·삭제 확인·그리드 패널·`flexRatio`
- [x] `code_list_extensions.dart` — `List<Code>` extension (콤보·표시명)
- [ ] 목록 툴바·선택행 보정·다이얼로그 submit 패턴 등 (필요 시)

### ③ 고객 (`ComCustomer`) — 완료

**목록·필터**

- [x] `GET /api/customers?aptCode=` — `aptCode` 생략 시 전체 · 값 있으면 `WHERE AptCode = aptCode` · `aptCode=`(빈 문자열)은 기타
- [x] 정렬: `AptCode, BuildingCode, FloorCode, RoomCode, CustName` (레거시 `SelectTblComCustomer`와 동일)
- [x] Flutter: `CustomerListPage` · 아파트 콤보(전체·기타) · 가상 그리드

**등록**

- [x] `POST /api/customers` — 채번 `C` + 4자리 (`MAX(CustCode)+1`)
- [x] 중복: 4코드 **모두 request `!= null`** 일 때만 위치 중복 검사 → `409`
- [x] 저장: 미선택 콤보 `null` → DB `""` (레거시 `BtnSave`와 동일)
- [x] Flutter: 등록 다이얼로그 · 연쇄 콤보 · 이름 자동 조합

**수정·삭제**

- [x] `PUT /api/customers/{custCode}` · `DELETE /api/customers/{custCode}`
- [x] 단건 `GET` 없음 — 수정은 그리드 행 값으로 다이얼로그 채움 (레거시 더블클릭과 동일)
- [x] 수정·삭제는 사전 `NOT_FOUND` 조회 없음
- [x] 수정 시 중복 검사 없음 (레거시 `ValidComCustomerData`에서 `comCustomer != null`이면 통과)
- [x] Flutter: 행 더블클릭 수정 · 선택 후 삭제 확인


### ④ 제품 (`ComProduct`) — 완료

**목록·필터**

- [x] `GET /api/products?processCode=&groupCode=` — 공정·그룹 **둘 다 필수** (레거시 `SelectTblComProductByProcessGroupCode`)
- [x] 정렬: `ORDER BY ProductCode`
- [x] Flutter: `ProductListPage` · 공정→그룹 연쇄 콤보 (`showAllOption: false`) · 그리드(제품명·단가)

**등록**

- [x] `POST /api/products` — 채번 `P` + 4자리 (`TOP 1 ORDER BY ProductCode DESC` + 1)
- [x] `processCode`·`groupCode`는 요청 body · 현재 필터값 고정 (다이얼로그 읽기전용)
- [x] Flutter: 등록 다이얼로그

**수정·삭제**

- [x] `PUT /api/products/{productCode}` · `DELETE /api/products/{productCode}`
- [x] 단건 `GET` 없음 — 수정은 그리드 행 값으로 다이얼로그 채움 (더블클릭)
- [x] 수정·삭제는 사전 `NOT_FOUND` 조회 없음
- [x] 수정 시 `ProductName`·`Price`만 갱신 (`ProcessCode`·`GroupCode` 변경 없음)
- [x] Flutter: 행 더블클릭 수정 · 선택 후 삭제 확인

### 알려진 이슈 · 2단계 전 후보

| 항목 | 상태 | 비고 |
|------|------|------|
| 셸 상태 유지·탭 | **2단계 착수 시** | **비즈니스**(접수·출고 등)만 `keepAlive` + 탭 · 기초·설정은 dispose 유지 |
| 공통 위젯 파일 분리 | **2단계 후** | `tk_sidebar.dart` 등 폴더 분리 (동작 변경 없음) |
| 목록 툴바·선택행 보정 | 필요 시 | 소규모 Flutter 리팩터 |

**1단계 완료**: 위 4개 마스터 + 문서 정합. [05_테스트.md](05_테스트.md) 1~4절 통과 목표.

---

## 2단계 — 접수 · 출고 · 매출 (1단계 후)

순서: **접수 → 출고 → 매출**

### 접수 (`OrderMaster` / `OrderDetail`)

- [ ] 목록 (기간·상태·검색) + 건수·총액 대조
- [ ] 상세, 등록·수정, 상태 변경

### 출고 (`DeliveryMaster` / `DeliveryDetail`)

- [ ] 목록·상세, 출고 처리

### 매출 (`SalesMaster` / `SalesDetail`)

- [ ] 목록·상세, 수납 (일반·선불·외상)

---

## 3단계 — 부가 (나중에)

일계표, 환경 설정, UX 개선, 인쇄

---

## 병행 운영 (2단계부터)

1. 같은 테이블을 양쪽에서 **동시에 수정하지 않음**
2. 아래 표처럼 **쓰기 담당** 정함
3. 건수·금액 **매일** 레거시와 대조

| 업무 | 레거시 | 차세대 |
|------|:------:|:------:|
| 회원·코드·고객·제품 | W → | W (1단계 후 이동) |
| 접수 | W | W (2단계) |
| 출고 | W | W |
| 매출/수납 | W | W |

**롤백**: API 중지 → 앱 중지 → 레거시로 업무

---

## 기술 요약

| 항목 | 내용 |
|------|------|
| 설정 | `application.yml` 하나, profile 없음, `localhost` |
| 로그 | 오류 JSON traceId · 파일 롤링 30일 — [02_아키텍처.md](02_아키텍처.md) |
| Java | `I*Service` + `*Service` |
| Flutter | Riverpod, 클린 아키텍처(단순화) |
| 디자인 | [06_디자인_시스템.md](06_디자인_시스템.md) |
| 레거시 | 형제 폴더에 소스 보관, 새 레포 미포함 |
